Create a new Shopify OS 2.0 section in shopify/sections/.

- Use Tailwind CSS only, no inline styles
- Include full schema with settings and blocks
- Add t: translation keys for all text
- Render via snippets where reusable
- Follow BEM-style naming for JS hooks (js-\*)
