Add a metafield reference to a Liquid template or section.

- Use shopify/config/metafields.json as source of truth
- Always check if metafield exists before rendering
- Use the meta-fields.liquid snippet for structured output
