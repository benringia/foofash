Write a Cart API interaction using fetch().

- Use the existing cart.js wrapper in src/
- No jQuery, no page reloads
- Handle errors gracefully with user feedback
- Optimistic UI update before response returns
